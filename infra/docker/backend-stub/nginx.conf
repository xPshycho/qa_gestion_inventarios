server {
  listen 8080;
  server_name _;

  location = /health {
    default_type application/json;
    return 200 '{"status":"UP","service":"backend-stub"}';
  }

  location / {
    default_type application/json;
    return 200 '{"message":"Backend stub ready for inventory API"}';
  }
}
