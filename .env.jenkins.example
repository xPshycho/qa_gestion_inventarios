# Contrato local de Jenkins. No escribir valores reales en este archivo.
# Generar .env.jenkins (ignorado y con permisos 0600) mediante:
# ./scripts/security/init-secret-env.sh jenkins

JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=
