services:
  postgres:
    ports:
      - "${POSTGRES_PORT:-55432}:5432"

  backend:
    ports:
      - "${BACKEND_PORT:-8080}:8080"

  keycloak:
    ports:
      - "${KEYCLOAK_PORT:-8081}:8080"

  frontend:
    ports:
      - "${FRONTEND_PORT:-5173}:8080"
